query findAllListingsBySlugCategory($findListingsInput: FindListingsInput!, $slug: String!) {
  findAllBySlugCategory(findListingsInput: $findListingsInput, slug: $slug) {
    id
    name
    price
    images
    createdAt
  }
}
