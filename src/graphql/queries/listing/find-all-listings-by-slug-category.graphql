query findAllListingsBySlugCategory($pagePagination: PagePagination!, $slug: String!) {
  findAllBySlugCategory(pagePagination: $pagePagination, slug: $slug) {
    id
    name
    price
    images
    createdAt
  }
}
